# Progress supplement

This is the chronological log for the extension modules that are now integrated into the
canonical `DelPezzoBlekherman/` hierarchy. References below to staging describe their historical
development state; the current integration outcome is recorded at the end.

## 2026-08-13

1. Re-read the full formalization instructions.
2. Implemented Proj functoriality under radical irrelevant-ideal containment and a genuine
   basepoint-free projective evaluation morphism.
3. Proved chartwise finiteness from a finite graded ring homomorphism.
4. Connected a `Fin (c+2)` free basis to finite projective morphism and rank `c+2`.
5. Proved reduced finite geometric fibers have exactly `c+2` points.
6. Proved trace-discriminant reducedness, discriminant base change, and reducedness away from
   the discriminant.
7. Proved the generic fiber of a finite injective domain map is a field and obtained automatic
   discriminant nonvanishing in characteristic zero.
8. Added the explicit `(1,c,1)` graded-basis certificate and carried it through the finite-map,
   discriminant, and geometric-fiber conclusions.
9. Classified finite reduced real-algebra closed points by residue degree one or two and stated
   the exact `c+2` real / `c` real plus one conjugate pair dichotomy.
10. Forced fresh compilation of the entire dependency chain, reran the placeholder scan, and
    reran the axiom audit.
11. Copied the verified source bundle into a non-overwriting workspace staging directory.
12. Formalized the Artinian Gorenstein degree-one core: perfect socle pairing, exact parameter
    dimension `m+1`, and Hankel rank `c` from explicit Hilbert-dimension inputs.
13. Formalized the inertia bridge that turns one negative direction per quadratic point into
    the at-most-one-conjugate-pair conclusion.
14. Formalized the fully-real reciprocal identity, its converse, and the exactly-one-negative
    coefficient theorem.
15. Formalized the one-conjugate-pair identity
    `∑ uᵢ²/aᵢ + 2 Re(v²/b) = 0`, its converse, its `b=α+βI` real-coordinate expansion, and
    the normalized `α<0` sign mechanism.
16. Recompiled the new modules to fresh outputs, extended the axiom audit, reran the placeholder
    scan, and refreshed this staged bundle.
17. Constructed the negative-direction certificate from explicit weighted coordinates and then
    specialized it to complex blocks `2 Re(bₚ zₚ²)` with `Re(bₚ)<0`.
18. Composed that construction with relation-kernel nonnegativity to prove the real-fiber
    dichotomy without a separate at-most-one-pair assumption, then reran the compiler and axiom
    audits and refreshed the staged bundle again.
19. Eliminated the remaining block-sign assumption in the normalized model: imaginary-axis test
    vectors derive `Re(bₚ)<0` from hyperplane nonnegativity and block non-isotropy, yielding an
    assumption-minimal at-most-one-pair theorem and exact real-fiber dichotomy.
20. Formalized the finite evaluation algebra of Proposition 5.2: uniqueness of the degree-one
    relation, the coefficient nonvanishing criterion, spanning of the full coordinate algebra by
    hyperplane products, and the resulting degree-two evaluation isomorphism.
21. Formalized the boundary SOS kernel argument and the `m+1` length lower bound, including a
    checked reduction of the spanning condition to the exact proper-subspace basepoint theorem
    plus strict positivity/no common zero.
22. Forced source recompilation (not merely dependent compilation) exposed and fixed several
    import-order and elaboration issues in the new evaluation, complex-block, and SOS modules;
    all three now compile to the normal build path and the expanded axiom audit exits cleanly.
23. Ran one ordered source rebuild of all eighteen modules followed by the complete axiom audit;
    the process exited zero. Reconciled the staged sources byte-for-byte with the verified scratch
    sources and reran the clean placeholder scan.
24. Added the convex separation layer: proper-cone Farkas separation, reduction of negativity to
    an extreme point of a compact base, and the dual characterization of the cone generated by
    squares. Proved that taking closure leaves the continuous dual unchanged.
25. Connected compact-base extreme points to the additive extreme-ray definition, including a
    verified theorem replacing any negative normalized separator by a negative extreme-ray
    generator.
26. Formalized the PSD kernel-face criterion and proved extremality of nonzero rank-one point
    evaluation forms by factoring every symmetric form vanishing on the evaluation hyperplane as
    a scalar multiple of `f ⊗ f`.
27. Composed proper-cone separation, finite dual representation, compact-base normalization,
    extreme-ray selection, and the point-evaluation/basepoint-free dichotomy into a checked final
    separation theorem conditional on the explicitly isolated geometric inputs.
28. Proved strict positivity gives no common zero for the summands of every SOS representation and
    concluded exact minimality for any supplied `m+1`-square representation; isolated Gram
    compression/existence of that representation as the remaining upper-bound input.
29. Completed that upper-bound input: proved span preservation for the rank-one update, finite
    list and finset iteration, extraction of a basis subfamily from an arbitrary finite family,
    and compression to exactly the finrank of the summand span. Composed this with the Hankel
    radical/basepoint argument to derive an `m+1`-square representation from any finite SOS
    representation and prove exact minimal length, without assuming the short representation.
30. Reduced the remaining projective basepoint input to projective surjectivity: proved every
    proper subspace has a nonzero annihilator, proved surjectivity on evaluation rays supplies a
    common zero, and composed this directly with the exact `m+1` SOS-length theorem.
31. Proved scheme-level surjectivity of the kernel morphism from the existing finite free
    certificate: injectivity descends to homogeneous degree-zero localizations, lying-over makes
    every affine chart map surjective, and Zariski-locality assembles global Proj surjectivity.
    Packaged finiteness, surjectivity, rank `c+2`, and nonzero discriminant for the `(1,c,1)`
    certificate.
32. Read the source PDF's proof of Theorem 8.3 and corrected the formal route: the paper does not
    assert that every proper radical subspace has a real basepoint. Formalized its actual
    perturbation argument instead: a short representation produces a projective direction outside
    the compact real image; density supplies a reduced fiber there; the `c+2` count and
    at-most-one-pair theorem force a real point and give the contradiction. Composed this lower
    bound with finite Gram compression to recover exact minimal length `m+1`.
33. Instantiated the perturbation theorem's abstract projective target by a concrete real model:
    the finite-dimensional dual unit sphere modulo the antipodal `±1` action. Proved this quotient
    compact Hausdorff, proved normalization and the direction map continuous, and proved direction
    equality is exactly nonzero real scaling. Also derived nowhere-zero restricted evaluation from
    strict positivity, and eliminated the abstract `pointOf`/ray-identification assumptions from
    the exact-minimal-length theorem.
34. Proved restriction of continuous dual functionals to the Hankel radical is itself a continuous
    linear map. Consequently, continuity of the ordinary finite-dimensional evaluation-functional
    map automatically gives continuity of the normalized real-projective kernel morphism.
35. Proved a general analytic nonvanishing-density theorem on preconnected normed spaces, then
    specialized it to nonzero real multivariate polynomials. Proved normalization onto the unit
    sphere and the antipodal quotient are surjective and transported density to real projective
    directions. Thus the PDF's reduced-open density step is checked once its nonzero discriminant
    is identified with a projective-coordinate polynomial.
36. Performed that identification at the certificate level: over `ℝ`, the checked `(1,c,1)`
    finite-free theorem supplies a nonzero discriminant in `MvPolynomial I ℝ`, and the new density
    theorem proves its represented projective nonvanishing locus is Euclidean dense.
37. Proved dense projective loci transport across continuous linear equivalences and packaged the
    coordinate equivalence supplied by any finite basis. Applied this to transport the certificate's
    discriminant locus to an arbitrary finite-dimensional target, including the continuous dual of
    the Hankel radical.
38. Named the certificate's real discriminant polynomial and its basis-transported projective
    open locus. Added a scalable composition theorem deriving nowhere-zero restricted evaluation
    from strict positivity and projective continuity from ordinary evaluation continuity, then
    concluding exact minimal length from any dense reduced locus. The checked certificate theorem
    supplies that locus directly; only concrete fiber point counts, the at-most-one-pair bound,
    and real-point/evaluation-image identification remain.
39. Proved the exact real-fiber count identity
    `finrank = #RationalClosedPoint + 2 * #QuadraticClosedPoint` for every reduced finite real
    algebra. Derived both the `c+2` equation consumed by the perturbation theorem and the terminal
    fact that `c≥1` plus at most one quadratic point forces a rational real point.
40. Defined `realCount` and `pairCount` for a projective family of finite real algebras. Proved
    pointwise reducedness and constant rank `c+2` automatically supply the exact count equation on
    any locus, while a pointwise quadratic-point bound supplies the perturbation pair-count input.
41. Proved a rational closed point in each fiber gives zero real count outside the real source
    image whenever it lifts compatibly to that source. Composed this with the family count theorem,
    ordinary evaluation continuity, projective density, and Gram compression to prove exact
    minimal length from an actual reduced constant-rank real fiber family.
42. Constructed the paper's coordinate-ring fiber family over concrete real projective directions
    as base change `ℝ ⊗[MvPolynomial I ℝ] C` along evaluation at a chosen representative. Verified
    commutative-ring, real-algebra, and finite-module instances; transported the `(1,c,1)` basis to
    prove rank `c+2`; and proved evaluated discriminant nonvanishing makes each fiber reduced.
43. Removed the representative mismatch without assuming discriminant homogeneity: defined a dense
    locus of projective directions equipped with affine coordinates where the discriminant is
    nonzero, and chose those coordinates on that locus.
44. Formed the corresponding good-coordinate tensor-product fiber family. Proved rank `c+2`
    everywhere and reducedness on the dense locus directly from the `(1,c,1)` basis and evaluated
    discriminant.
45. Lifted the normalized complex-block at-most-one-pair calculation uniformly to this dependent
    fiber family, isolating only the concrete block coefficients, relation-kernel nonnegativity,
    and block non-isotropy.
46. Proved every rational closed point of a finite algebra induces a base-field-valued algebra
    point. Restricted a rational tensor-product fiber point to the source coordinate ring and
    proved its polynomial parameters equal the coordinates defining the fiber.
47. Specialized that lift to the `(1,c,1)` family: its values on the degree-one generators are
    exactly the chosen affine coordinates, are nonzero on the good locus, and recover the original
    real projective direction. Derived zero real-fiber count outside the projective image from a
    compatible realization of source algebra points in `X(ℝ)`.
48. Tightened the perturbation interfaces to require the zero-count/lift property only on the
    dense locus actually used by the proof. Forced a fresh ordered rebuild of all 47 source
    modules, reran the expanded axiom audit, verified matching staged hashes, and reran the clean
    placeholder scan.
49. Tightened the geometric realization bridge again: it now asks only for realization of real
    algebra points whose degree-one generator vector is nonzero, exactly the projective locus, and
    does not require an artificial realization of the affine origin.
50. Defined the canonical source of nonzero real algebra points and its generator-direction map.
    Proved the outside-image zero-count theorem for this source with no lift or realization
    hypothesis, narrowing the geometric bridge to comparison with the actual `X(ℝ)` realization.
51. Proved that continuity of the full finite-dimensional evaluation-functional map follows from
    continuity on the vectors of any finite basis. Compiled the new 48th module and reran the
    expanded axiom audit.
52. Tightened the Artinian Gorenstein rank bridge to the exact functional used in PDF Theorem 4.3.
    Proved every nonzero functional on the one-dimensional socle is injective, that composition
    with it preserves the perfect degree-one multiplication pairing, and that nonzeroness of the
    actual pulled-back Hankel form suffices to derive radical dimension `m+1` and rank `c` without
    choosing a socle coordinate. Rebuilt the full library and expanded the axiom audit.
53. Transported the quotient result back to the original ambient Hankel form. Proved that its
    pointwise factorization through quotient multiplication is equality with the canonical
    pullback, then identified its actual kernel with the parameter space and derived kernel
    dimension `m+1` and rank `c`. Added both declarations to the axiom audit.
54. Constructed the quotient data appearing in that factorization. Ambient degree-one
    multiplication now descends through `R₁/W` in both arguments when products involving `W`
    lie in a degree-two relation space `J`; an ambient functional annihilating `J` descends
    through `R₂/J`. Proved the representative factorization, descended symmetry, and an
    ambient-data endpoint identifying the original Hankel kernel with `W` and computing its
    dimension/rank as `m+1`/`c`.
55. Removed the separate nonzero-Hankel hypothesis from the strongest ambient endpoint. Proved
    that surjectivity of the linearized multiplication `R₁ ⊗ R₁ → R₂` makes composition
    with multiplication injective on degree-two functionals; consequently the PDF's original
    `ell ≠ 0` input supplies a nonzero Hankel form and the exact kernel/dimension/rank theorem.

### Integration outcome

The supplemental modules were initially kept in a non-overwriting staging directory to preserve
the existing implementation while filesystem permissions were restricted. On 2026-08-13 a Git
baseline captured that state, after which all verified modules were renamed into the canonical
subject hierarchy and added to the root import graph. The integrated project then passed a full
`lake build`, the placeholder scan, and the separate axiom audit.
